# Конфигурация Streamlit для UI агента (ui/app.py).

[client]
# Прячем верхнюю панель «Deploy» и гамбургер-меню — чище для демо/учебного стенда.
toolbarMode = "minimal"

[theme]
# Чуть мягче дефолтного: акцент в тон банковской теме.
primaryColor = "#2f6f4f"
