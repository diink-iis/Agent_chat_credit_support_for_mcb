# Секреты — НЕ коммитим (шаблон .env.example остаётся в гите)
.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# OS
.DS_Store

# streamlit: тему (config.toml) коммитим, секреты — нет
.streamlit/secrets.toml

# Сохранённые диалоги UI (переписка + checkpointer графа) — локальные, не коммитим
ui/.sessions/
